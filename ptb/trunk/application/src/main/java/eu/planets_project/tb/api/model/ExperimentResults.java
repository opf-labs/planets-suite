package eu.planets_project.tb.api.model;

public interface ExperimentResults {
	
	//Not yet clear in which form "experiment results" are packed and from where to retrieve
	
	public boolean isEvaluated();
	public void setisEvaluated(boolean evaluated);
	
}
