package eu.planets_project.tb.api.services.mockups;

import java.util.List;

public interface ServiceRegistry {
	
	//TODO: define mockup for service registry
	/* getAllAvailableServiceNames();
	 * getAllAvailableServiceIDs();
	 * 
	 */
	
	/**The ‘Tool Type’ and ‘Object Type’ fields will consist of select boxes containing the available
	 * tool and object types, e.g. specify a ‘jpeg2pdfMigration’ experiment on ‘jpeg’ images – no
	 * reference to actual tools or data that is part of the Design Experiment stage (stage 2).
	 * */
	//public List<String> getAllAvailableToolTypes(String s);
	
	

}
